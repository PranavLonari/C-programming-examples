// Online C compiler to run C program online
#include <stdio.h>

int main() {
    // Write C code here
    int n,arm=0,r,c;
    printf("Enter a number");
    scanf("%d",&n);
    c=n;
    while(n>0)
    {
        r=n%10;
        arm= (r*r*r) + arm;
        n=n/10;
    }
    if(c==arm)
    {
        printf("armstrong");
    }
    else
    {
        printf("Not armstrong");
    }
    return 0;
}
output:

Enter a number 
152
Not armstrong
